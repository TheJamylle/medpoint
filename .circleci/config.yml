version: 2.1
orbs:
  node: circleci/node@3.0.0

jobs:
  some:
    executor:
      name: node/default
      steps:
        - checkout
        - node/with-cache:
            steps:
              - run: npm install
              - run: npm run build
  another:
    executor:
      name: node/default
    steps:
      - run: 
          command: | 
            echo "Trying somethig new"
            mkdir aha

workflows:
  node-tests:
    jobs:
      - node/test

      - some

      - another:
          requires:
            - some