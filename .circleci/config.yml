version: 2.1
orbs:
  node: circleci/node@3.0.0

jobs:
  some:
    executor:
      name: node/default
    steps:
      - run: echo "ACHO QUE FUNFOU"

workflows:
  node-tests:
    jobs:
      - node/test

      - some